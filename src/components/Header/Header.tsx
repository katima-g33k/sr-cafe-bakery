import React from "react";
import Logo from "@/components/Logo/Logo";

export default function Header() {
  return (
    <header className="text-red-400 bg-red-500">
      <Logo />
      Header
    </header>
  );
}
